knp_paginator:
  template:
    pagination: "pagination/custom_pagination.html.twig"
  default_options:
    page_name: page
    sort_field_name: sort
    sort_direction_name: direction
    distinct: true

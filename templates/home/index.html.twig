{% extends 'base.html.twig' %}

{% block body %}
<!-- Hero Carousel -->
<div class="relative overflow-hidden">
    <div class="absolute inset-0 z-0">
        {% for key,header in headers %}
        <div class="absolute inset-0 transition-opacity duration-1000 ease-in-out {% if key != 0 %}opacity-0{% endif %}"
            data-carousel-item="{{ key }}">
            <img src="/uploads/{{ header.illustration }}" class="w-full h-full object-cover object-center"
                alt="{{ header.title }}">
            <div class="absolute inset-0 bg-terracota-900/40"></div>
        </div>
        {% endfor %}
    </div>

    <!-- Indicators -->
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 z-10 flex space-x-2">
        {% for key,header in headers %}
        <button
            class="w-3 h-3 rounded-full bg-white/30 hover:bg-white/50 transition-colors {% if key == 0 %}bg-white/80{% endif %}"
            data-carousel-indicator="{{ key }}"></button>
        {% endfor %}
    </div>

    <!-- Content -->
    <div class="relative z-10 h-full flex items-center">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            {% for key,header in headers %}
            <div class="{% if key != 0 %}hidden{% endif %} space-y-6 text-white" data-carousel-content="{{ key }}">
                <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold">{{ header.title }}</h1>
                <p class="text-xl max-w-2xl mx-auto">{{ header.content }}</p>
                <a href="{{ header.buttonLink }}"
                    class="inline-block px-8 py-3 bg-terracota-600 hover:bg-terracota-700 text-white font-medium rounded-lg transition-colors">
                    {{ header.buttonTitle }}
                </a>
            </div>
            {% endfor %}
        </div>
    </div>
</div>

<!-- Featured Products -->
<section class="py-16 bg-terracota-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl font-bold text-center text-terracota-800 mb-12">Nos créations phares</h2>

        <div class="grid md:grid-cols-3 gap-8">
            {% for product in productsInHomepage %}
            <div class="group relative bg-white rounded-xl shadow-lg hover:shadow-xl transition-shadow">
                <div class="aspect-square overflow-hidden rounded-t-xl">
                    <img src="/uploads/{{ product.illustration }}"
                        class="w-full h-full object-cover group-hover:scale-105 transition-transform"
                        alt="{{ product.name }}">
                </div>
                <div class="p-6">
                    <h3 class="text-xl font-semibold text-terracota-700">{{ product.name }}</h3>
                    <div class="mt-2 text-terracota-600 prose prose-sm">
                        {{ product.description|raw }}
                    </div>
                    <a href="{{ path('app_product', { slug : product.slug } ) }}"
                        class="mt-4 inline-block px-4 py-2 bg-terracota-100 hover:bg-terracota-200 text-terracota-700 rounded-md transition-colors">
                        Découvrir
                    </a>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Featurettes -->
<section class="py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-24">
        <div class="flex flex-col md:flex-row items-center gap-12">
            <div class="md:w-1/2">
                <img src="path/to/artisan-image.jpg" class="w-full h-96 object-cover rounded-xl shadow-lg"
                    alt="Artisanat local">
            </div>
            <div class="md:w-1/2">
                <h2 class="text-3xl font-bold text-terracota-800 mb-4">Savoir-faire artisanal</h2>
                <p class="text-lg text-terracota-600 leading-relaxed">
                    Découvrez les techniques ancestrales transmises de génération en génération par nos artisans
                    passionnés.
                </p>
            </div>
        </div>

        <div class="flex flex-col md:flex-row-reverse items-center gap-12">
            <div class="md:w-1/2">
                <img src="path/to/materials-image.jpg" class="w-full h-96 object-cover rounded-xl shadow-lg"
                    alt="Matériaux naturels">
            </div>
            <div class="md:w-1/2">
                <h2 class="text-3xl font-bold text-terracota-800 mb-4">Matériaux 100% naturels</h2>
                <p class="text-lg text-terracota-600 leading-relaxed">
                    Des matières premières locales et durables, sélectionnées avec soin pour leur qualité et leur
                    authenticité.
                </p>
            </div>
        </div>

        <div class="flex flex-col md:flex-row items-center gap-12">
            <div class="md:w-1/2">
                <img src="path/to/community-image.jpg" class="w-full h-96 object-cover rounded-xl shadow-lg"
                    alt="Communauté">
            </div>
            <div class="md:w-1/2">
                <h2 class="text-3xl font-bold text-terracota-800 mb-4">Une communauté engagée</h2>
                <p class="text-lg text-terracota-600 leading-relaxed">
                    Rejoignez un mouvement qui valorise le travail équitable et préserve les traditions culturelles.
                </p>
            </div>
        </div>
    </div>
</section>
{% endblock %}